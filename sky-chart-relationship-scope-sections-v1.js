// Group the visible relationship list and its Copy serializer by literal sky scope.
(function(){
'use strict';
if(!/(^|\/)sky-chart\.html$/.test(location.pathname)||window.__relphiRelationshipScopeSectionsV1)return;
window.__relphiRelationshipScopeSectionsV1=true;

const GROUPS=Object.freeze([{mode:'A-A',title:'A↔A'},{mode:'B-B',title:'B↔B'},{mode:'A-B',title:'A↔B'}]);
const PLACEMENT_SYMBOLS=Object.freeze({sun:'☉',moon:'☽',mercury:'☿',venus:'♀',mars:'♂',jupiter:'♃',saturn:'♄',uranus:'♅',neptune:'♆',pluto:'♇',chiron:'⚷','north-node':'☊','south-node':'☋',lilith:'⚸','part-of-fortune':'⊗',vertex:'Vx',asc:'Asc',dsc:'Dsc',mc:'MC',ic:'IC'});
const SIGN_SYMBOLS=Object.freeze(['♈','♉','♊','♋','♌','♍','♎','♏','♐','♑','♒','♓']);
const ASPECT_SYMBOLS=Object.freeze({conjunction:'☌',opposition:'☍',trine:'△',square:'□',sextile:'✶','semi-sextile':'⚺',quincunx:'⚻',octile:'∠','tri-octile':'⚼',quintile:'Q','bi-quintile':'BQ'});
let observer=null,observedList=null,queued=false,applying=false;

function installStyles(){if(document.getElementById('skyRelationshipScopeSectionsV1Styles'))return;const style=document.createElement('style');style.id='skyRelationshipScopeSectionsV1Styles';style.textContent=`
  .sky-relationship-scope-heading{grid-column:1/-1;display:flex;align-items:center;width:100%;min-height:28px;box-sizing:border-box;padding:7px 10px 5px;color:#514941;background:#f4efe8;border-top:1px solid rgba(31,27,24,.13);border-bottom:1px solid rgba(31,27,24,.08);font:900 .63rem/1 system-ui,sans-serif;letter-spacing:.01em}
  .sky-relationship-scope-heading:first-child{border-top:0}
  .sky-relationship-scope-heading[hidden]{display:none!important}
`;document.head.appendChild(style)}
function mode(row){const explicit=String(row?.dataset?.relationshipMode||'').toUpperCase();if(explicit==='A-A'||explicit==='B-B'||explicit==='A-B')return explicit;if(explicit==='B-A')return'A-B';const left=String(row?.dataset?.leftSky||'A').toUpperCase(),right=String(row?.dataset?.rightSky||'B').toUpperCase();if(left===right&&left==='A')return'A-A';if(left===right&&left==='B')return'B-B';return'A-B'}
function visible(row){return!!row&&!row.hidden&&row.getAttribute('aria-hidden')!=='true'&&getComputedStyle(row).display!=='none'}
function heading(list,group){let node=list.querySelector(`:scope>.sky-relationship-scope-heading[data-relationship-scope-heading="${group.mode}"]`);if(!node){node=document.createElement('div');node.className='sky-relationship-scope-heading';node.dataset.relationshipScopeHeading=group.mode;node.textContent=group.title;node.setAttribute('role','heading');node.setAttribute('aria-level','3')}return node}
function sameOrder(a,b){return a.length===b.length&&a.every((node,index)=>node===b[index])}
function groupList(){queued=false;if(applying)return;const list=document.getElementById('skyFoundationRelationshipList');if(!list)return;installStyles();const rows=[...list.querySelectorAll(':scope>.sky-foundation-relationship-row')];if(!rows.length)return;const other=[...list.children].filter(node=>!node.matches?.('.sky-foundation-relationship-row,.sky-relationship-scope-heading'));const desired=[...other];GROUPS.forEach(group=>{const section=rows.filter(row=>mode(row)===group.mode),head=heading(list,group);head.hidden=!section.some(visible);desired.push(head,...section)});const actual=[...list.children];if(!sameOrder(actual,desired)){applying=true;const fragment=document.createDocumentFragment();desired.forEach(node=>fragment.appendChild(node));list.appendChild(fragment);applying=false}else GROUPS.forEach(group=>{const head=list.querySelector(`:scope>.sky-relationship-scope-heading[data-relationship-scope-heading="${group.mode}"]`),section=rows.filter(row=>mode(row)===group.mode);if(head)head.hidden=!section.some(visible)})}
function schedule(){if(queued)return;queued=true;requestAnimationFrame(()=>requestAnimationFrame(groupList))}
function coordinate(row,side){const group=row.querySelector(`.sky-foundation-relationship-placement--${side}`),small=group?.querySelector('.sky-foundation-relationship-copy small');const stored=String(small?.dataset?.relationshipCoordinate||'').trim();if(stored)return stored;return String(small?.textContent||'').match(/\d{1,2}°\d{2}′/)?.[0]||''}
function placementSymbol(id){return PLACEMENT_SYMBOLS[id]||window.RelphiGlyphRegistry?.get?.(id)?.fallback||id}
function serializeRow(row){const leftId=String(row.dataset.leftPlacement||''),rightId=String(row.dataset.rightPlacement||''),aspect=String(row.dataset.aspect||''),leftCoordinate=coordinate(row,'left'),rightCoordinate=coordinate(row,'right'),leftSign=SIGN_SYMBOLS[Number(row.dataset.leftSign)]||'',rightSign=SIGN_SYMBOLS[Number(row.dataset.rightSign)]||'';if(!leftId||!rightId||!leftCoordinate||!rightCoordinate)return'';return`${placementSymbol(leftId)} in ${leftSign} ${leftCoordinate}  ${ASPECT_SYMBOLS[aspect]||aspect}  ${placementSymbol(rightId)} in ${rightSign} ${rightCoordinate}`.replace(/\s+/g,' ').trim()}
function visibleRows(){return[...document.querySelectorAll('#skyFoundationRelationshipList>.sky-foundation-relationship-row[data-relation-index]')].filter(visible)}
function serializeAll(){const rows=visibleRows(),sections=GROUPS.map(group=>{const lines=rows.filter(row=>mode(row)===group.mode).map(serializeRow).filter(Boolean);return lines.length?`${group.title}\n${lines.join('\n')}`:''}).filter(Boolean);return sections.length?`Relationships\n\n${sections.join('\n\n')}`:''}
async function writeClipboard(text){try{if(navigator.clipboard?.writeText){await navigator.clipboard.writeText(text);return true}}catch(_){}const textarea=document.createElement('textarea');textarea.value=text;textarea.setAttribute('readonly','');Object.assign(textarea.style,{position:'fixed',left:'-10000px',top:'0',opacity:'0'});document.body.appendChild(textarea);textarea.select();let ok=false;try{ok=document.execCommand('copy')}catch(_){}textarea.remove();return ok}
function bindCopyButton(){const button=document.querySelector('.sky-relationship-copy-button');if(!button||button.dataset.scopeCopyOverride==='true')return;button.dataset.scopeCopyOverride='true';button.setAttribute('aria-label','Copy visible relationships grouped as A to A, B to B, and A to B');button.addEventListener('click',async event=>{event.preventDefault();event.stopImmediatePropagation();const text=serializeAll();if(!text)return;const ok=await writeClipboard(text);if(!ok)return;const original='Copy';button.textContent='Copied';window.setTimeout(()=>{if(button.isConnected)button.textContent=original},1400)},true)}
function bind(){const list=document.getElementById('skyFoundationRelationshipList');if(list&&list!==observedList){observer?.disconnect();observedList=list;observer=new MutationObserver(records=>{if(applying)return;if(records.some(record=>record.type==='childList'||record.type==='attributes'))schedule()});observer.observe(list,{childList:true,subtree:true,attributes:true,attributeFilter:['class','hidden','aria-hidden']})}bindCopyButton();schedule()}
function start(){installStyles();bind();['relphi:sky-foundation-ready','relphi:sky-foundation-interactions-ready','relphi:sky-intrasky-relationships-ready','relphi:sky-intrasky-b-relationships-ready','relphi:sky-aspect-multiselect-changed','relphi:sky-placement-multiselect-changed','relphi:sky-house-multiselect-changed','relphi:sky-zodiac-filter-changed','relphi:sky-foundation-filter-changed'].forEach(name=>window.addEventListener(name,bind));new MutationObserver(bind).observe(document.body,{childList:true,subtree:true})}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start,{once:true}):start();
})();
