// Secondary-progressions playback and active temporal annotations for Sky Chart.
(function(){
  'use strict';
  if(!/(^|\/)sky-chart\.html$/.test(location.pathname)||window.__relphiSkyProgressionsV1)return;
  window.__relphiSkyProgressionsV1=true;

  const core=window.RelphiSkyProgressionsCore;
  if(!core)return;
  const {DAY,YEAR,SIGNS,ASPECTS,norm,wrap,aspectError,secondaryProgressedMs,signState,classifyMotion,activeRelationships}=core;
  const NS='http://www.w3.org/2000/svg';
  const KEYS={A:'relphiSkyChartA',B:'relphiSkyChartB'};
  const SKY={A:'#c9211e',B:'#2462d0'};
  const COLORS=['#e53935','#f06b32','#f39a2e','#f5be3d','#f1dc43','#a9cf46','#43a85b','#2ca69b','#3285c7','#5961c8','#8c4fb4','#bd438e'];
  const BODIES=['sun','moon','mercury','venus','mars','jupiter','saturn','uranus','neptune','pluto'];
  const BODY_NAME={sun:'Sun',moon:'Moon',mercury:'Mercury',venus:'Venus',mars:'Mars',jupiter:'Jupiter',saturn:'Saturn',uranus:'Uranus',neptune:'Neptune',pluto:'Pluto'};
  const BODY_LABEL=Object.fromEntries(BODIES.map(id=>[id,id.replace(/(^|-)([a-z])/g,(_,dash,char)=>`${dash?' ':''}${char.toUpperCase()}`)]));
  const FILTER_DEFAULTS={ingress:true,egress:true,retrograde:true,direct:true,intra:true,inter:true};
  const state={source:'A',reference:'natal',rangeStart:NaN,rangeEnd:NaN,target:NaN,playing:false,speed:30,filters:{...FILTER_DEFAULTS},corridor:1,renderId:0,raf:0,lastFrame:0,aspectWindowCache:new Map(),speedScale:new Map(),lastStorageSignature:''};
  const markerGroups={progressed:new Map(),reference:new Map()};

  const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[char]));
  const clamp=(value,min,max)=>Math.max(min,Math.min(max,value));
  const read=slot=>{try{return JSON.parse(localStorage.getItem(KEYS[slot])||'null')}catch(_){return null}};
  const profile=payload=>payload?.calcProfile&&typeof payload.calcProfile==='object'?payload.calcProfile:payload||{};
  const epochMs=payload=>{const raw=profile(payload)?.instant||profile(payload)?.dateTime||payload?.instant||payload?.dateTime;if(!raw)return NaN;const value=new Date(raw).getTime();return Number.isFinite(value)?value:NaN};
  const fmtDate=ms=>new Intl.DateTimeFormat(undefined,{month:'short',day:'numeric',year:'numeric'}).format(new Date(ms));
  const fmtDateTime=ms=>new Intl.DateTimeFormat(undefined,{month:'short',day:'numeric',year:'numeric',hour:'numeric',minute:'2-digit'}).format(new Date(ms));
  const dateInputValue=ms=>{const d=new Date(ms),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return`${y}-${m}-${day}`};
  const parseDateInput=value=>{const m=String(value||'').match(/^(\d{4})-(\d{2})-(\d{2})$/);if(!m)return NaN;return new Date(Number(m[1]),Number(m[2])-1,Number(m[3]),12,0,0,0).getTime()};
  const degreeLabel=value=>{let total=Math.round(Math.abs(Number(value))*60),degree=Math.floor(total/60);total%=60;return`${degree}°${String(total).padStart(2,'0')}′`};
  const ageLabel=(epoch,target)=>{const years=(target-epoch)/YEAR;if(!Number.isFinite(years))return'';return years<2?`${years.toFixed(2)} years`:`${years.toFixed(1)} years`};
  const storageSignature=()=>`${localStorage.getItem(KEYS.A)||''}|${localStorage.getItem(KEYS.B)||''}`;
  const canonicalId=value=>String(value||'').trim().toLowerCase().replace(/[_\s]+/g,'-');

  function placementSource(payload){
    if(!payload||typeof payload!=='object')return[];
    const known=[payload.placements,payload.positions,payload.points,payload.bodies].find(value=>value&&typeof value==='object'),source=known||payload;
    if(Array.isArray(source))return source.map((item,index)=>[String(item?.id||item?.name||item?.label||index),item]);
    return Object.entries(source).filter(([key,value])=>value&&typeof value==='object'&&!Array.isArray(value)&&!/^(calcProfile|metadata|profile|location|notes|houseCusps|cusps|houses)$/i.test(key));
  }
  function itemLongitude(item){
    if(Number.isFinite(Number(item?.longitude)))return norm(item.longitude);
    const sign=SIGNS.findIndex(name=>name.toLowerCase()===String(item?.sign||item?.zodiac||'').trim().toLowerCase());
    if(sign<0)return NaN;
    return norm(sign*30+Number(item?.degree||item?.degrees||0)+Number(item?.minute||item?.minutes||0)/60+Number(item?.second||item?.seconds||0)/3600);
  }
  function storedRecords(payload){
    const aliases={sol:'sun',luna:'moon'},registry=window.RelphiGlyphRegistry,byId=new Map();
    for(const [key,item] of placementSource(payload)){
      const candidates=[item?.glyphId,item?.id,item?.name,item?.label,item?.body,item?.planet,key].filter(Boolean);let id='';
      for(const candidate of candidates){const raw=canonicalId(candidate),resolved=registry&&(registry.resolve(raw)||registry.get(raw)),test=canonicalId(resolved?.id||aliases[raw]||raw);if(BODIES.includes(test)){id=test;break}}
      const value=itemLongitude(item);if(id&&Number.isFinite(value)&&!byId.has(id))byId.set(id,{id,name:BODY_LABEL[id],value});
    }
    return BODIES.map(id=>byId.get(id)).filter(Boolean);
  }
  function sourcePayload(){return read(state.source)}
  function otherSlot(){return state.source==='A'?'B':'A'}
  function referencePayload(){return state.reference==='other'?read(otherSlot()):sourcePayload()}
  function referenceRecords(){return storedRecords(referencePayload())}
  function sourceEpoch(){return epochMs(sourcePayload())}
  function astronomyBody(id){return window.Astronomy?.Body?.[BODY_NAME[id]]||BODY_NAME[id]}
  function longitudeAtProgressed(id,progressedMs){const astronomy=window.Astronomy;if(!astronomy)return NaN;const vector=astronomy.GeoVector(astronomyBody(id),new Date(progressedMs),true);return norm(astronomy.Ecliptic(vector).elon)}
  function progressedLongitude(id,targetMs){const epoch=sourceEpoch();return longitudeAtProgressed(id,secondaryProgressedMs(epoch,targetMs))}
  function progressedRecords(targetMs){return BODIES.map(id=>({id,name:BODY_LABEL[id],value:progressedLongitude(id,targetMs)})).filter(record=>Number.isFinite(record.value))}
  function progressedSpeed(id,targetMs){const epoch=sourceEpoch(),p=secondaryProgressedMs(epoch,targetMs),step=.04*DAY;return wrap(longitudeAtProgressed(id,p+step)-longitudeAtProgressed(id,p-step))/(.08)}
  function speedAtProgressed(id,pMs){const step=.04*DAY;return wrap(longitudeAtProgressed(id,pMs+step)-longitudeAtProgressed(id,pMs-step))/(.08)}
  function defaultRange(){const epoch=sourceEpoch();if(!Number.isFinite(epoch))return;const now=Date.now(),center=Math.max(epoch,now),fiveYears=5*YEAR;state.rangeStart=Math.max(epoch,center-fiveYears);state.rangeEnd=center+fiveYears;state.target=clamp(now,state.rangeStart,state.rangeEnd);state.aspectWindowCache.clear();state.speedScale.clear()}
  function ensureValidSource(){if(Number.isFinite(epochMs(read(state.source))))return true;const alternative=state.source==='A'?'B':'A';if(Number.isFinite(epochMs(read(alternative)))){state.source=alternative;return true}return false}
  function polar(radius,degree){const angle=(degree-180)*Math.PI/180;return{x:260+radius*Math.cos(angle),y:260+radius*Math.sin(angle)}}
  function annular(inner,outer,start,end){const span=((end-start)%360+360)%360||360,large=span>180?1:0,a=polar(outer,start),b=polar(outer,start+span),c=polar(inner,start+span),d=polar(inner,start);return`M${a.x} ${a.y} A${outer} ${outer} 0 ${large} 1 ${b.x} ${b.y} L${c.x} ${c.y} A${inner} ${inner} 0 ${large} 0 ${d.x} ${d.y} Z`}
  function buildWheel(svg){
    svg.replaceChildren();const zodiac=document.createElementNS(NS,'g');zodiac.setAttribute('class','sky-progression-zodiac');svg.appendChild(zodiac);
    for(let i=0;i<12;i+=1){const path=document.createElementNS(NS,'path');path.setAttribute('d',annular(214,244,i*30,(i+1)*30));path.setAttribute('fill',COLORS[i]);path.setAttribute('opacity','.9');zodiac.appendChild(path);const p=polar(229,i*30+15),text=document.createElementNS(NS,'text');text.setAttribute('x',p.x);text.setAttribute('y',p.y);text.setAttribute('text-anchor','middle');text.setAttribute('dominant-baseline','middle');text.setAttribute('class','sky-progression-sign-label');text.textContent=SIGNS[i].slice(0,3);zodiac.appendChild(text);const boundaryA=polar(145,i*30),boundaryB=polar(244,i*30),line=document.createElementNS(NS,'line');line.setAttribute('x1',boundaryA.x);line.setAttribute('y1',boundaryA.y);line.setAttribute('x2',boundaryB.x);line.setAttribute('y2',boundaryB.y);line.setAttribute('class','sky-progression-sign-boundary');zodiac.appendChild(line)}
    for(const radius of [145,170,198,214,244]){const circle=document.createElementNS(NS,'circle');circle.setAttribute('cx','260');circle.setAttribute('cy','260');circle.setAttribute('r',String(radius));circle.setAttribute('class','sky-progression-guide');svg.appendChild(circle)}
    const aspects=document.createElementNS(NS,'g');aspects.dataset.progressionAspectLines='';svg.appendChild(aspects);const reference=document.createElementNS(NS,'g');reference.dataset.progressionReferenceMarkers='';svg.appendChild(reference);const progressed=document.createElementNS(NS,'g');progressed.dataset.progressionProgressedMarkers='';svg.appendChild(progressed);const center=document.createElementNS(NS,'g');center.setAttribute('class','sky-progression-center');const t1=document.createElementNS(NS,'text');t1.setAttribute('x','260');t1.setAttribute('y','252');t1.setAttribute('text-anchor','middle');t1.dataset.progressionCenterDate='';center.appendChild(t1);const t2=document.createElementNS(NS,'text');t2.setAttribute('x','260');t2.setAttribute('y','274');t2.setAttribute('text-anchor','middle');t2.dataset.progressionCenterAge='';center.appendChild(t2);svg.appendChild(center)
  }
  async function ensureMarker(groupMap,layer,id,color,opacity){let g=groupMap.get(id);if(g&&g.isConnected)return g;g=document.createElementNS(NS,'g');g.classList.add('sky-progression-marker');g.dataset.body=id;g.style.opacity=String(opacity);layer.appendChild(g);groupMap.set(id,g);const registry=window.RelphiGlyphRegistry,component=window.RelphiGlyphComponent,entry=registry&&(registry.get(id)||registry.resolve(id));if(entry&&component?.draw){try{await component.draw(g,entry.id,{radius:14,padding:1,color});g.dataset.canonicalGlyph='true'}catch(error){g.dataset.glyphError=String(error?.message||error)}}return g}
  async function updateMarkers(svg,progressed,reference){const progressedLayer=svg.querySelector('[data-progression-progressed-markers]'),referenceLayer=svg.querySelector('[data-progression-reference-markers]'),sourceColor=SKY[state.source],refColor=state.reference==='other'?SKY[otherSlot()]:sourceColor,activeP=new Set(progressed.map(r=>r.id)),activeR=new Set(reference.map(r=>r.id));for(const [id,g] of markerGroups.progressed)g.hidden=!activeP.has(id);for(const [id,g] of markerGroups.reference)g.hidden=!activeR.has(id);await Promise.all(progressed.map(async record=>{const g=await ensureMarker(markerGroups.progressed,progressedLayer,record.id,sourceColor,1),p=polar(198,record.value);g.hidden=false;g.setAttribute('transform',`translate(${p.x} ${p.y})`);g.setAttribute('aria-label',`Progressed ${record.name}`)}));await Promise.all(reference.map(async record=>{const g=await ensureMarker(markerGroups.reference,referenceLayer,record.id,refColor,.34),p=polar(164,record.value);g.hidden=false;g.setAttribute('transform',`translate(${p.x} ${p.y})`);g.setAttribute('aria-label',`${referenceLabel()} ${record.name}`)}))}
  function referenceLabel(){if(state.reference==='other')return read(otherSlot())?.name||`Sky ${otherSlot()}`;return`Natal ${read(state.source)?.name||`Sky ${state.source}`}`}
  function currentOrb(){const raw=Number(document.querySelector('[data-filter="orb"]')?.value);return Number.isFinite(raw)&&raw>=0?raw:1}
  function visibleAspectRows(progressed,reference){const orb=currentOrb(),rows=[];if(state.filters.intra)rows.push(...activeRelationships(progressed,progressed,orb,{mode:'intra'}));if(state.filters.inter&&reference.length)rows.push(...activeRelationships(progressed,reference,orb,{mode:'inter'}));return rows.sort((a,b)=>a.error-b.error||a.aspect.angle-b.aspect.angle).slice(0,32)}
  function relationKey(relation){return`${state.source}|${state.reference}|${relation.mode}|${relation.left.id}|${relation.right.id}|${relation.aspect.id}|${currentOrb().toFixed(3)}`}
  function relationErrorAt(relation,targetMs,referenceMap){const a=progressedLongitude(relation.left.id,targetMs),b=relation.mode==='intra'?progressedLongitude(relation.right.id,targetMs):referenceMap.get(relation.right.id)?.value;return Number.isFinite(a)&&Number.isFinite(b)?aspectError(a,b,relation.aspect.angle):Infinity}
  function refineBoundary(relation,a,b,referenceMap,orb){let fa=relationErrorAt(relation,a,referenceMap)-orb;for(let i=0;i<24;i+=1){const m=(a+b)/2,fm=relationErrorAt(relation,m,referenceMap)-orb;if(Math.sign(fm)===Math.sign(fa)){a=m;fa=fm}else b=m}return(a+b)/2}
  function aspectWindowFor(relation,targetMs,referenceMap){const key=relationKey(relation),cached=state.aspectWindowCache.get(key);if(cached&&targetMs>=cached.start&&targetMs<=cached.end)return cached;const orb=currentOrb(),step=14*DAY,maxSpan=80*YEAR;let left=targetMs,right=targetMs,probe=targetMs;while(targetMs-left<maxSpan){const next=probe-step;if(relationErrorAt(relation,next,referenceMap)>orb){left=refineBoundary(relation,next,probe,referenceMap,orb);break}probe=next;left=probe}probe=targetMs;while(right-targetMs<maxSpan){const next=probe+step;if(relationErrorAt(relation,next,referenceMap)>orb){right=refineBoundary(relation,probe,next,referenceMap,orb);break}probe=next;right=probe}const result={start:left,end:right};state.aspectWindowCache.set(key,result);return result}
  function aspectMotion(relation,targetMs,referenceMap){return classifyMotion(relationErrorAt(relation,targetMs-DAY,referenceMap),relationErrorAt(relation,targetMs+DAY,referenceMap),.0005)}
  function speedScale(id,targetMs){const epoch=sourceEpoch(),p=secondaryProgressedMs(epoch,targetMs),key=`${id}|${Math.round(p/(10*DAY))}`;if(state.speedScale.has(key))return state.speedScale.get(key);let max=0;for(let offset=-20;offset<=20;offset+=5)max=Math.max(max,Math.abs(speedAtProgressed(id,p+offset*DAY)));const threshold=max*.12;state.speedScale.set(key,threshold);return threshold}
  function nearestStation(id,targetMs){const epoch=sourceEpoch(),center=secondaryProgressedMs(epoch,targetMs),step=.25*DAY,candidates=[];function root(a,b){let fa=speedAtProgressed(id,a);for(let i=0;i<24;i+=1){const m=(a+b)/2,fm=speedAtProgressed(id,m);if(Math.sign(fm)===Math.sign(fa)){a=m;fa=fm}else b=m}return(a+b)/2}let prev=center-5*DAY,prevSpeed=speedAtProgressed(id,prev);for(let p=prev+step;p<=center+5*DAY;p+=step){const speed=speedAtProgressed(id,p);if(Math.sign(speed)!==Math.sign(prevSpeed))candidates.push(root(prev,p));prev=p;prevSpeed=speed}if(!candidates.length)return null;const station=candidates.reduce((best,value)=>Math.abs(value-center)<Math.abs(best-center)?value:best,candidates[0]),target=core.targetMsFromProgressedMs(epoch,station),after=speedAtProgressed(id,station+.08*DAY);return{progressedMs:station,targetMs:target,type:after<0?'retrograde':'direct'}}
  function stationAnnotation(id,targetMs){if(id==='sun'||id==='moon')return null;const speed=progressedSpeed(id,targetMs),threshold=speedScale(id,targetMs);if(!Number.isFinite(speed)||!Number.isFinite(threshold)||threshold<=0||Math.abs(speed)>threshold)return null;const station=nearestStation(id,targetMs);if(!station)return null;return{id:`station-${id}-${station.type}`,category:station.type,title:station.type==='retrograde'?`${BODY_LABEL[id]} beginning retrograde`:`${BODY_LABEL[id]} ending retrograde`,meta:`Station ${station.type==='retrograde'?'Rx':'direct'} · ${fmtDate(station.targetMs)} · speed ${Math.abs(speed).toFixed(3)}°/day`,sort:station.targetMs}}
  function signAnnotations(progressed,targetMs){const rows=[];for(const record of progressed){const speed=progressedSpeed(record.id,targetMs),status=signState(record.value,speed,state.corridor);if(!status.kind||!state.filters[status.kind])continue;const degree=status.degree.toFixed(2),verb=status.kind==='ingress'?'entering':'leaving',direction=status.direction==='retrograde'?' · retrograde':'';rows.push({id:`${status.kind}-${record.id}`,category:status.kind,title:`${record.name} ${verb} ${status.sign}`,meta:`${status.kind[0].toUpperCase()+status.kind.slice(1)} · ${degree}° ${status.sign}${direction} · ${state.corridor}° active window`,progress:status.progress,sort:targetMs})}return rows}
  function aspectAnnotations(relations,targetMs,referenceMap){return relations.map(relation=>{const mode=relation.mode,window=aspectWindowFor(relation,targetMs,referenceMap),motion=aspectMotion(relation,targetMs,referenceMap),left=`Progressed ${relation.left.name}`,right=mode==='intra'?`progressed ${relation.right.name}`:`${referenceLabel()} ${relation.right.name}`;return{id:`${mode}-${relation.left.id}-${relation.right.id}-${relation.aspect.id}`,category:mode,title:`${left} ${relation.aspect.name.toLowerCase()} ${right}`,meta:`${mode==='intra'?'Intra':'Inter'} · orb ${degreeLabel(relation.error)} · ${motion} · ${fmtDate(window.start)}–${fmtDate(window.end)}`,sort:window.start,aspect:relation.aspect,relation}})}
  function renderAspectLines(svg,relations,progressedMap,referenceMap){const layer=svg.querySelector('[data-progression-aspect-lines]');layer.replaceChildren();for(const relation of relations){const aRecord=progressedMap.get(relation.left.id),bRecord=relation.mode==='intra'?progressedMap.get(relation.right.id):referenceMap.get(relation.right.id);if(!aRecord||!bRecord)continue;const a=polar(198,aRecord.value),b=polar(relation.mode==='intra'?198:164,bRecord.value),line=document.createElementNS(NS,'line');line.setAttribute('x1',a.x);line.setAttribute('y1',a.y);line.setAttribute('x2',b.x);line.setAttribute('y2',b.y);line.setAttribute('stroke',relation.aspect.color);line.setAttribute('class',`sky-progression-aspect-line is-${relation.mode}`);line.dataset.relationKey=relationKey(relation);layer.appendChild(line)}}
  function annotationMarkup(item){const progress=Number.isFinite(item.progress)?`<div class="sky-progression-annotation-progress" aria-hidden="true"><i style="--progress:${Math.round(item.progress*100)}%"></i></div>`:'';return`<article class="sky-progression-annotation is-${esc(item.category)}" data-progression-annotation="${esc(item.id)}"><div class="sky-progression-annotation-time">${esc(fmtDate(state.target))}</div><strong>${esc(item.title)}</strong><span>${esc(item.meta)}</span>${progress}</article>`}
  function renderAnnotations(host,items){if(!items.length){host.innerHTML='<p class="sky-progression-no-events">No enabled temporal conditions are active at this point on the timeline.</p>';return}host.innerHTML=items.sort((a,b)=>a.sort-b.sort||a.title.localeCompare(b.title)).slice(0,24).map(annotationMarkup).join('')}
  async function render(){const id=++state.renderId,panel=document.getElementById('skyProgressionsPanel');if(!panel||panel.hidden)return;const svg=panel.querySelector('[data-progression-wheel]'),annotationHost=panel.querySelector('[data-progression-annotations]'),epoch=sourceEpoch();if(!Number.isFinite(epoch)||!window.Astronomy){panel.dataset.progressionAvailable='false';annotationHost.innerHTML='<p class="sky-progression-no-events">A stored sky with a valid instant and Astronomy Engine is required.</p>';return}panel.dataset.progressionAvailable='true';const progressed=progressedRecords(state.target),reference=referenceRecords(),pMap=new Map(progressed.map(r=>[r.id,r])),rMap=new Map(reference.map(r=>[r.id,r]));await updateMarkers(svg,progressed,reference);if(id!==state.renderId)return;const relations=visibleAspectRows(progressed,reference);renderAspectLines(svg,relations,pMap,rMap);const items=[...signAnnotations(progressed,state.target)];if(state.filters.retrograde||state.filters.direct){for(const body of BODIES){const item=stationAnnotation(body,state.target);if(item&&state.filters[item.category])items.push(item)}}items.push(...aspectAnnotations(relations,state.target,rMap));if(id!==state.renderId)return;renderAnnotations(annotationHost,items);svg.querySelector('[data-progression-center-date]').textContent=fmtDate(state.target);svg.querySelector('[data-progression-center-age]').textContent=`Age ${ageLabel(epoch,state.target)}`;panel.querySelector('[data-progression-date-label]').textContent=fmtDateTime(state.target);panel.querySelector('[data-progression-age-label]').textContent=`Secondary progression · ${ageLabel(epoch,state.target)} after epoch`;syncScrubber()}
  function scheduleRender(){cancelAnimationFrame(state.raf);state.raf=requestAnimationFrame(render)}
  function syncScrubber(){const panel=document.getElementById('skyProgressionsPanel');if(!panel)return;const slider=panel.querySelector('[data-progression-scrubber]'),span=Math.max(1,Math.round((state.rangeEnd-state.rangeStart)/DAY)),value=clamp(Math.round((state.target-state.rangeStart)/DAY),0,span);slider.max=String(span);slider.value=String(value);panel.querySelector('[data-progression-range-start]').value=dateInputValue(state.rangeStart);panel.querySelector('[data-progression-range-end]').value=dateInputValue(state.rangeEnd)}
  function setTarget(ms){state.target=clamp(ms,state.rangeStart,state.rangeEnd);scheduleRender()}
  function setRange(start,end){const epoch=sourceEpoch();start=Math.max(epoch,start);if(!Number.isFinite(start)||!Number.isFinite(end)||end<=start+DAY)return false;state.rangeStart=start;state.rangeEnd=end;state.target=clamp(state.target,start,end);state.aspectWindowCache.clear();state.speedScale.clear();syncScrubber();scheduleRender();return true}
  function stop(){state.playing=false;state.lastFrame=0;const button=document.querySelector('[data-progression-play]');if(button){button.dataset.playing='false';button.textContent='Play';button.setAttribute('aria-label','Play progressions')}}
  function play(){if(state.playing){stop();return}state.playing=true;state.lastFrame=performance.now();const button=document.querySelector('[data-progression-play]');if(button){button.dataset.playing='true';button.textContent='Pause';button.setAttribute('aria-label','Pause progressions')}function frame(now){if(!state.playing)return;const elapsed=(now-state.lastFrame)/1000;state.lastFrame=now;const next=state.target+elapsed*state.speed*DAY;if(next>=state.rangeEnd){setTarget(state.rangeEnd);stop();return}state.target=next;scheduleRender();requestAnimationFrame(frame)}requestAnimationFrame(frame)}
  function panelMarkup(){const filter=(id,label)=>`<button type="button" class="sky-progression-filter is-on" data-progression-filter="${id}" aria-pressed="true">${label}</button>`;return`<section id="skyProgressionsPanel" class="sky-progressions-panel" aria-label="Progressions" hidden><div class="sky-progressions-toolbar"><label>Progress <select data-progression-source aria-label="Progression source"><option value="A">Sky A</option><option value="B">Sky B</option></select></label><span class="sky-progressions-method">Secondary · 1 day = 1 year</span><label>Inter reference <select data-progression-reference aria-label="Inter-aspect reference"><option value="natal">Natal source</option><option value="other">Other sky</option></select></label></div><div class="sky-progressions-stage"><div class="sky-progressions-wheel-shell"><svg data-progression-wheel class="sky-progression-wheel" viewBox="0 0 520 520" role="img" aria-label="Animated secondary progression wheel"></svg></div><aside class="sky-progression-commentary" aria-label="Active temporal annotations"><header><span>Active now</span><strong data-progression-date-label></strong><small data-progression-age-label></small></header><div class="sky-progression-annotations" data-progression-annotations></div></aside></div><div class="sky-progression-filters" aria-label="Progression annotation filters">${filter('ingress','Ingress')}${filter('egress','Egress')}${filter('retrograde','Begin retrograde')}${filter('direct','End retrograde')}${filter('intra','Intra aspects')}${filter('inter','Inter aspects')}</div><div class="sky-progression-playback"><div class="sky-progression-range"><label>From <input type="date" data-progression-range-start></label><label>To <input type="date" data-progression-range-end></label></div><div class="sky-progression-scrub-row"><button type="button" data-progression-play>Play</button><input type="range" min="0" max="3650" step="1" value="0" data-progression-scrubber aria-label="Progression timeline"><select data-progression-speed aria-label="Playback speed"><option value="30">1 month/s</option><option value="90">3 months/s</option><option value="365.2422">1 year/s</option></select><button type="button" data-progression-now>Today</button></div><p class="sky-progression-rule">Ingress and egress annotations remain visible only inside the ${state.corridor}° threshold. Aspect annotations remain visible only while the relationship is inside the current Sky Chart orb.</p></div></section>`}
  function enhance(){const middle=document.getElementById('skyFoundationComparison');if(!middle||document.getElementById('skyProgressionsPanel'))return;const heading=middle.querySelector(':scope > .sky-foundation-heading'),tabs=document.createElement('div');tabs.className='sky-comparison-tabs';tabs.setAttribute('role','tablist');tabs.setAttribute('aria-label','Middle panel view');tabs.innerHTML='<button type="button" role="tab" aria-selected="true" data-sky-middle-tab="comparison">Comparison</button><button type="button" role="tab" aria-selected="false" data-sky-middle-tab="progressions">Progressions</button>';heading?.insertAdjacentElement('afterend',tabs);middle.insertAdjacentHTML('beforeend',panelMarkup());const panel=document.getElementById('skyProgressionsPanel'),svg=panel.querySelector('[data-progression-wheel]');buildWheel(svg);if(!ensureValidSource())panel.dataset.progressionAvailable='false';defaultRange();panel.querySelector('[data-progression-source]').value=state.source;panel.querySelector('[data-progression-reference]').value=state.reference;syncScrubber();middle.dataset.progressionsEnhanced='true'}
  function activate(name){const middle=document.getElementById('skyFoundationComparison'),panel=document.getElementById('skyProgressionsPanel');if(!middle||!panel)return;const progression=name==='progressions';middle.dataset.progressionsActive=progression?'true':'false';panel.hidden=!progression;middle.querySelectorAll('[data-sky-middle-tab]').forEach(button=>button.setAttribute('aria-selected',button.dataset.skyMiddleTab===name?'true':'false'));if(progression){stop();refreshFromStorage();scheduleRender()}else stop()}
  function refreshFromStorage(){const signature=storageSignature();if(signature===state.lastStorageSignature)return;state.lastStorageSignature=signature;if(!ensureValidSource())return;defaultRange();state.aspectWindowCache.clear();state.speedScale.clear();const source=document.querySelector('[data-progression-source]');if(source)source.value=state.source;const otherOption=document.querySelector('[data-progression-reference] option[value="other"]');if(otherOption)otherOption.disabled=!read(otherSlot());syncScrubber()}
  document.addEventListener('click',event=>{const tab=event.target.closest?.('[data-sky-middle-tab]');if(tab){activate(tab.dataset.skyMiddleTab);return}const filter=event.target.closest?.('[data-progression-filter]');if(filter){const key=filter.dataset.progressionFilter;state.filters[key]=!state.filters[key];filter.classList.toggle('is-on',state.filters[key]);filter.setAttribute('aria-pressed',state.filters[key]?'true':'false');scheduleRender();return}if(event.target.closest?.('[data-progression-play]')){play();return}if(event.target.closest?.('[data-progression-now]')){setTarget(clamp(Date.now(),state.rangeStart,state.rangeEnd));return}});
  document.addEventListener('input',event=>{if(event.target.matches?.('[data-progression-scrubber]')){stop();setTarget(state.rangeStart+Number(event.target.value)*DAY)}});
  document.addEventListener('change',event=>{if(event.target.matches?.('[data-progression-source]')){stop();state.source=event.target.value==='B'?'B':'A';state.reference='natal';document.querySelector('[data-progression-reference]').value='natal';defaultRange();markerGroups.progressed.clear();markerGroups.reference.clear();const svg=document.querySelector('[data-progression-wheel]');if(svg)buildWheel(svg);scheduleRender();return}if(event.target.matches?.('[data-progression-reference]')){state.reference=event.target.value==='other'?'other':'natal';state.aspectWindowCache.clear();markerGroups.reference.clear();const svg=document.querySelector('[data-progression-wheel]');if(svg)svg.querySelector('[data-progression-reference-markers]')?.replaceChildren();scheduleRender();return}if(event.target.matches?.('[data-progression-speed]')){state.speed=Number(event.target.value)||30;return}if(event.target.matches?.('[data-progression-range-start], [data-progression-range-end]')){const panel=document.getElementById('skyProgressionsPanel'),start=parseDateInput(panel.querySelector('[data-progression-range-start]').value),end=parseDateInput(panel.querySelector('[data-progression-range-end]').value);if(!setRange(start,end))syncScrubber();return}});
  window.addEventListener('relphi:sky-orb-limit-changed',()=>{state.aspectWindowCache.clear();scheduleRender()});window.addEventListener('relphi:sky-foundation-ready',()=>{enhance();refreshFromStorage()});setInterval(()=>{if(document.getElementById('skyFoundationComparison')?.dataset.progressionsActive==='true')refreshFromStorage()},1000);
  function start(){enhance();state.lastStorageSignature=storageSignature();refreshFromStorage()}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start,{once:true}):start();
})();
