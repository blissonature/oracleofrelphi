name: Planetary Hours settings UI temp

on:
  push:
    branches:
      - fix/planetary-hours-visual-parity
    paths:
      - '.github/workflows/ph-settings-ui-temp.yml'

permissions:
  contents: write

jobs:
  edit-settings-ui:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          ref: fix/planetary-hours-visual-parity
          fetch-depth: 0
      - name: Improve Where and when settings UI at source
        run: python .github/ph_settings_ui_temp.py
      - name: Run Planetary Hours visual ownership contract
        run: node tests/planetary-hours-sky-chart-visual-parity.mjs
      - name: Commit product change and remove temporary editor
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/ph_settings_ui_temp.py .github/workflows/ph-settings-ui-temp.yml
          git add planetaryhours.html tests/planetary-hours-sky-chart-visual-parity.mjs
          git commit -m 'Improve Planetary Hours where and when settings UI'
          git push origin HEAD:fix/planetary-hours-visual-parity
