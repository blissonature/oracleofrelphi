# Canonical glyphs v1 staging

Review-only, incomplete, fail-closed source package. It contains 33 of 93 exact masters and the exact Circled backplate. Missing identities use null paths. Day-ruler, hour-ruler, and combined day-hour overlays are unavailable. Nothing in production consumes this directory.

Validate with `node tools/verify-canonical-source-package.mjs <this-directory>`.
