// Unified relationship list: A↔B, A↔A, and B↔B coexist by default.
// Intrasky rows use the same harmonic candidate ceiling and the same downstream filters
// as inter-sky rows. A compact Scope filter can exclude any of the three relationship kinds.
(function(){
'use strict';
if(!/(^|\/)sky-chart\.html$/.test(location.pathname)||window.__relphiSkyIntraskyRelationshipsV1)return;
window.__relphiSkyIntraskyRelationshipsV1=true;

const KEYS={A:'relphiSkyChartA',B:'relphiSkyChartB'};
const COLORS={A:'#c9211e',B:'#2462d0'};
const SIGNS=['aries','taurus','gemini','cancer','leo','virgo','libra','scorpio','sagittarius','capricorn','aquarius','pisces'];
const SIGN_NAMES=['Aries','Taurus','Gemini','Cancer','Leo','Virgo','Libra','Scorpio','Sagittarius','Capricorn','Aquarius','Pisces'];
const ORDER=['sun','moon','asc','mercury','venus','mars','jupiter','saturn','uranus','neptune','pluto','north-node','south-node','chiron','lilith','part-of-fortune','vertex','mc','ic','dsc'];
const ALIASES={rising:'asc',ascendant:'asc',asc:'asc',ac:'asc',descendant:'dsc',dsc:'dsc',dc:'dsc',midheaven:'mc',mc:'mc','imum coeli':'ic',imumcoeli:'ic',ic:'ic',vertex:'vertex',vx:'vertex','north node':'north-node',node:'north-node','true node':'north-node','mean node':'north-node','south node':'south-node',chiron:'chiron',lilith:'lilith','black moon lilith':'lilith',fortune:'part-of-fortune','part of fortune':'part-of-fortune',pof:'part-of-fortune'};
const SCOPE_KEY='relphiSkyRelationshipScopesV1';
const ALL_SCOPES=['inter','intra-a','intra-b'];
const HIDDEN_CLASSES=['sky-foundation-single-sky-cross-hidden','sky-chart-filter-hidden','sky-chart-orb-hidden','sky-orb-filter-hidden','sky-chart-multiselect-hidden','sky-chart-house-multiselect-hidden','sky-chart-aspect-multiselect-hidden','sky-chart-sign-filter-hidden','sky-chart-semantic-hidden','sky-chart-scope-hidden'];
let queued=false,countQueued=false,colorQueued=false,listObserver=null;

const norm=value=>((Number(value)%360)+360)%360;
const separation=(a,b)=>Math.abs(((a-b+180)%360+360)%360-180);
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
function read(slot){try{return JSON.parse(localStorage.getItem(KEYS[slot])||'null')}catch(_){return null}}
function source(payload){if(!payload||typeof payload!=='object')return[];const known=[payload.placements,payload.positions,payload.points,payload.bodies].find(value=>value&&typeof value==='object'),raw=known||payload;if(Array.isArray(raw))return raw.map((item,index)=>[String(item?.name||item?.label||item?.id||index),item]);return Object.entries(raw).filter(([key,item])=>item&&typeof item==='object'&&!Array.isArray(item)&&!/^(calcProfile|metadata|profile|location|notes|houseCusps|cusps|houses)$/i.test(key)&&(Number.isFinite(Number(item.longitude))||item.sign||item.zodiac))}
function longitude(item){if(Number.isFinite(Number(item?.longitude)))return norm(item.longitude);const sign=SIGNS.indexOf(String(item?.sign||item?.zodiac||'').trim().toLowerCase());return sign<0?NaN:norm(sign*30+Number(item.degree||item.degrees||0)+Number(item.minute||item.minutes||0)/60+Number(item.second||item.seconds||0)/3600)}
function canonical(key,item){const registry=window.RelphiGlyphRegistry;if(!registry)return null;for(const candidate of [item?.glyphId,item?.id,item?.name,item?.label,item?.body,item?.planet,item?.point,key]){if(candidate==null)continue;const raw=String(candidate).trim(),id=ALIASES[raw.toLowerCase()]||raw,entry=registry.resolve?.(id)||registry.get?.(id);if(entry)return entry}return null}
function profile(payload){return payload?.calcProfile&&typeof payload.calcProfile==='object'?payload.calcProfile:{}}
function ascendant(payload,list){const record=list.find(item=>item.id==='asc');if(record)return record.value;const value=Number(profile(payload).ascendant??payload?.ascendant??payload?.asc);return Number.isFinite(value)?norm(value):0}
function cusps(payload,list){const p=profile(payload);for(const raw of [p.houseCusps,p.cusps,payload?.houseCusps,payload?.cusps,payload?.houses]){if(!raw)continue;const values=(Array.isArray(raw)?raw:Object.values(raw)).map(item=>typeof item==='object'?Number(item.longitude??item.value??item.cusp):Number(item)).slice(0,12);if(values.length===12&&values.every(Number.isFinite))return values.map(norm)}const asc=ascendant(payload,list),system=String(p.houseSystem||payload?.houseSystem||'whole-sign').toLowerCase(),start=system.includes('whole')?Math.floor(asc/30)*30:asc;return Array.from({length:12},(_,index)=>norm(start+index*30))}
function houseFor(value,houseCusps){for(let index=0;index<12;index+=1){const start=houseCusps[index],span=norm(houseCusps[(index+1)%12]-start)||30;if(norm(value-start)<span)return index+1}return 12}
function prepare(slot){const payload=read(slot),list=source(payload).map(([key,item])=>{const entry=canonical(key,item),value=longitude(item);return{key,item,entry,id:entry?.id||'',value,sky:slot}}).filter(record=>record.entry&&Number.isFinite(record.value)).sort((a,b)=>{const ai=ORDER.indexOf(a.id),bi=ORDER.indexOf(b.id);return(ai<0?999:ai)-(bi<0?999:bi)||a.value-b.value}),houseCusps=cusps(payload,list);list.forEach(record=>{record.sign=Math.floor(record.value/30);record.house=houseFor(record.value,houseCusps)});return list}
function relationships(list,scope){const harmonic=window.RelphiHarmonicOrb,aspects=harmonic?.aspects||[],windowValue=Number(harmonic?.maxWindow)||12,result=[];for(let leftIndex=0;leftIndex<list.length;leftIndex+=1){for(let rightIndex=leftIndex+1;rightIndex<list.length;rightIndex+=1){const left=list[leftIndex],right=list[rightIndex],distance=separation(left.value,right.value);for(const aspect of aspects){const relation=harmonic?.relation?.(left,right,aspect,distance,windowValue);if(relation)result.push({...relation,scope})}}}return result.sort((a,b)=>a.phaseError-b.phaseError||a.harmonicOrder-b.harmonicOrder||a.orb-b.orb)}
function coordinate(record){const item=record.item||{},explicitSign=SIGNS.indexOf(String(item.sign||item.zodiac||'').trim().toLowerCase()),explicitDegree=Number(item.degree??item.degrees),explicitMinute=Number(item.minute??item.minutes);if(explicitSign>=0&&Number.isFinite(explicitDegree)&&Number.isFinite(explicitMinute)){const degree=Math.max(0,Math.min(29,Math.trunc(explicitDegree))),minute=Math.max(0,Math.min(59,Math.trunc(explicitMinute)));return{sign:explicitSign,text:`${degree}°${String(minute).padStart(2,'0')}′`}}const sign=Math.floor(record.value/30),within=record.value-sign*30,degree=Math.floor(within),minute=Math.floor((within-degree)*60+1e-9);return{sign,text:`${degree}°${String(minute).padStart(2,'0')}′`}}
function glyphSlot(role,label){const slot=document.createElement('span');slot.className=`sky-foundation-relationship-glyph sky-foundation-relationship-glyph--${role}`;slot.dataset.glyphRole=role;slot.setAttribute('aria-label',label);return slot}
function makeRow(relation,index){const slot=relation.left.sky,scope=relation.scope,left=coordinate(relation.left),right=coordinate(relation.right),row=document.createElement('button');row.type='button';row.className='sky-foundation-relationship-row sky-intrasky-generated';row.dataset.relationshipSelection='true';row.dataset.relationIndex=`${slot}${slot}-${index}`;row.dataset.relationshipMode=`${slot}-${slot}`;row.dataset.relationScope=scope;row.dataset.leftSky=slot;row.dataset.rightSky=slot;row.dataset.aspect=relation.aspect.id;row.dataset.leftPlacement=relation.left.id;row.dataset.rightPlacement=relation.right.id;row.dataset.sourceOrb=relation.orb.toFixed(6);row.dataset.harmonicOrder=String(relation.harmonicOrder);row.dataset.harmonicNumerator=String(relation.harmonicNumerator);row.dataset.phaseError=relation.phaseError.toFixed(6);row.dataset.signedPhaseError=relation.signedPhaseError.toFixed(6);row.dataset.harmonicWindow=relation.masterWindow.toFixed(6);row.dataset.windowFraction=Number.isFinite(relation.windowFraction)?relation.windowFraction.toFixed(6):String(relation.windowFraction);row.dataset.harmonicCoherence=relation.coherence.toFixed(8);row.dataset.leftHouse=String(relation.left.house);row.dataset.rightHouse=String(relation.right.house);row.dataset.leftSign=String(left.sign);row.dataset.rightSign=String(right.sign);row.setAttribute('aria-label',`Sky ${slot} ${relation.left.entry.name} ${relation.aspect.id} Sky ${slot} ${relation.right.entry.name}, orb ${relation.orb.toFixed(2)} degrees, harmonic ${relation.harmonicOrder}, phase error ${relation.phaseError.toFixed(2)} degrees, coherence ${relation.coherencePercent.toFixed(0)} percent`);const leftGlyph=glyphSlot('left',relation.left.entry.name),aspectGlyph=glyphSlot('aspect',relation.aspect.id),rightGlyph=glyphSlot('right',relation.right.entry.name),leftCopy=document.createElement('span'),rightCopy=document.createElement('span');leftCopy.className=rightCopy.className='sky-foundation-relationship-copy';leftCopy.innerHTML=`${esc(relation.left.entry.name)}<small>${left.text} ${esc(SIGN_NAMES[left.sign])} · H${relation.left.house}</small>`;rightCopy.innerHTML=`${esc(relation.right.entry.name)}<small>${right.text} ${esc(SIGN_NAMES[right.sign])} · H${relation.right.house} · Orb ${relation.orb.toFixed(2)}°</small>`;row.append(leftGlyph,leftCopy,aspectGlyph,rightGlyph,rightCopy);return row}
function annotateInterRows(list){list.querySelectorAll(':scope>.sky-foundation-relationship-row:not(.sky-intrasky-generated)').forEach(row=>{row.dataset.relationshipMode='A-B';row.dataset.relationScope='inter';row.dataset.leftSky='A';row.dataset.rightSky='B'})}
function sortRows(list){const rows=[...list.querySelectorAll(':scope>.sky-foundation-relationship-row')];rows.sort((a,b)=>Number(a.dataset.phaseError||Infinity)-Number(b.dataset.phaseError||Infinity)||Number(a.dataset.harmonicOrder||Infinity)-Number(b.dataset.harmonicOrder||Infinity)||Number(a.dataset.sourceOrb||Infinity)-Number(b.dataset.sourceOrb||Infinity)||String(a.dataset.relationIndex).localeCompare(String(b.dataset.relationIndex)));rows.forEach(row=>list.appendChild(row))}
function readScopes(){try{const raw=JSON.parse(localStorage.getItem(SCOPE_KEY)||'null');if(Array.isArray(raw)){const valid=raw.filter(scope=>ALL_SCOPES.includes(scope));if(valid.length)return new Set(valid)}}catch(_){}return new Set(ALL_SCOPES)}
function saveScopes(scopes){try{localStorage.setItem(SCOPE_KEY,JSON.stringify([...scopes]))}catch(_){}}
function scopeFor(row){if(row.dataset.relationScope)return row.dataset.relationScope;const left=row.dataset.leftSky||'A',right=row.dataset.rightSky||'B';return left===right?(left==='A'?'intra-a':'intra-b'):'inter'}
function ensureScopeControl(){const bar=document.querySelector('#skyFoundationRelationships .sky-chart-filter-bar');if(!bar)return null;let control=bar.querySelector('[data-relationship-scope-filter]');if(control)return control;control=document.createElement('div');control.className='sky-relationship-scope-filter';control.dataset.relationshipScopeFilter='true';const title=document.createElement('span');title.className='sky-relationship-scope-label';title.textContent='Scope';const choices=document.createElement('div');choices.className='sky-relationship-scope-choices';choices.setAttribute('role','group');choices.setAttribute('aria-label','Relationship scope');[['inter','A↔B'],['intra-a','A↔A'],['intra-b','B↔B']].forEach(([value,labelText])=>{const label=document.createElement('label');label.className='sky-relationship-scope-choice';const input=document.createElement('input');input.type='checkbox';input.value=value;input.dataset.relationshipScopeChoice=value;input.setAttribute('aria-label',labelText);const text=document.createElement('span');text.textContent=labelText;label.append(input,text);choices.appendChild(label)});control.append(title,choices);const display=bar.querySelector('[data-relationship-display-control]');display?bar.insertBefore(control,display):bar.appendChild(control);control.addEventListener('change',event=>{const input=event.target.closest('[data-relationship-scope-choice]');if(!input)return;const scopes=new Set([...control.querySelectorAll('[data-relationship-scope-choice]:checked')].map(node=>node.value));saveScopes(scopes);applyScope(scopes)});return control}
function syncScopeChecks(scopes){const control=ensureScopeControl();control?.querySelectorAll('[data-relationship-scope-choice]').forEach(input=>{input.checked=scopes.has(input.value)})}
function applyScope(scopes=readScopes()){syncScopeChecks(scopes);document.querySelectorAll('#skyFoundationRelationshipList .sky-foundation-relationship-row').forEach(row=>row.classList.toggle('sky-chart-scope-hidden',!scopes.has(scopeFor(row))));document.querySelectorAll('#skyFoundationWheelMount [data-layer="aspects"] .sky-foundation-aspect').forEach(line=>line.classList.toggle('sky-chart-scope-hidden',!scopes.has('inter')));document.documentElement.dataset.skyRelationshipScopes=[...scopes].join(',');scheduleCount();window.dispatchEvent(new CustomEvent('relphi:sky-relationship-scope-changed',{detail:{scopes:[...scopes]}}))}
function updateCount(){countQueued=false;const rows=[...document.querySelectorAll('#skyFoundationRelationshipList .sky-foundation-relationship-row')],shown=rows.filter(row=>!row.hidden&&!HIDDEN_CLASSES.some(className=>row.classList.contains(className))&&getComputedStyle(row).display!=='none').length,count=document.getElementById('skyFoundationRelationshipCount'),empty=document.getElementById('skyFoundationRelationshipEmpty');if(count){count.textContent=`${shown}/${rows.length}`;count.dataset.total=String(rows.length)}if(empty)empty.hidden=shown!==0}
function scheduleCount(){if(countQueued)return;countQueued=true;requestAnimationFrame(()=>requestAnimationFrame(updateCount))}
async function repairSlot(row,side){const templates=window.RelphiRelationshipGlyphTemplates;if(!templates?.clone)return;const sky=row.dataset[`${side}Sky`],color=COLORS[sky];if(!color)return;const placement=row.dataset[`${side}Placement`],glyph=row.querySelector(`.sky-foundation-relationship-glyph--${side}`);if(glyph&&placement){const desired=`${placement}|${color}|plain`;if(glyph.dataset.templateKey!==desired||!glyph.firstElementChild){const clone=await templates.clone(placement,color);if(clone&&row.isConnected){glyph.dataset.templateKey=desired;glyph.replaceChildren(clone)}}}const signIndex=Number(row.dataset[`${side}Sign`]),sign=SIGNS[signIndex],signSlot=row.querySelector(`.sky-foundation-relationship-placement--${side} .sky-foundation-relationship-sign`);if(signSlot&&sign){const desired=`${sign}|${color}|plain`;if(signSlot.dataset.templateKey!==desired||!signSlot.firstElementChild){const clone=await templates.clone(sign,color);if(clone&&row.isConnected){signSlot.dataset.templateKey=desired;signSlot.replaceChildren(clone)}}}}
function repairColors(){colorQueued=false;document.querySelectorAll('#skyFoundationRelationshipList .sky-intrasky-generated').forEach(row=>{repairSlot(row,'left');repairSlot(row,'right')})}
function scheduleColors(){if(colorQueued)return;colorQueued=true;requestAnimationFrame(()=>requestAnimationFrame(repairColors))}
function installStyles(){if(document.getElementById('skyIntraskyRelationshipsV1Styles'))return;const style=document.createElement('style');style.id='skyIntraskyRelationshipsV1Styles';style.textContent=`
  .sky-chart-scope-hidden{display:none!important}
  #skyFoundationRelationships .sky-relationship-scope-filter{align-self:end;display:grid;grid-template-rows:auto var(--sky-filter-height,35px);gap:4px;min-width:0;color:var(--sky-filter-label-color,#4e463f);font:var(--sky-filter-label-font,800 .62rem/1.2 system-ui,sans-serif)}
  #skyFoundationRelationships .sky-relationship-scope-label{align-self:end;white-space:nowrap}
  #skyFoundationRelationships .sky-relationship-scope-choices{display:flex;align-items:center;gap:7px;min-width:0;height:var(--sky-filter-height,35px);padding:.35rem .48rem;border:var(--sky-filter-border,1px solid rgba(31,27,24,.2));border-radius:var(--sky-filter-radius,9px);background:#fff;box-sizing:border-box;overflow:hidden}
  #skyFoundationRelationships .sky-relationship-scope-choice{display:inline-flex;align-items:center;gap:3px;min-width:0;margin:0;font:700 .62rem/1 system-ui,sans-serif;white-space:nowrap}
  #skyFoundationRelationships .sky-relationship-scope-choice input{flex:0 0 14px;width:14px;height:14px;margin:0}
  #skyFoundationRelationshipList .sky-intrasky-generated[data-left-sky="A"]{--intrasky-color:${COLORS.A}}
  #skyFoundationRelationshipList .sky-intrasky-generated[data-left-sky="B"]{--intrasky-color:${COLORS.B}}
  #skyFoundationRelationshipList .sky-intrasky-generated::after{content:attr(data-relationship-mode);position:absolute;right:6px;top:5px;color:var(--intrasky-color);font:900 .48rem/1 system-ui,sans-serif;letter-spacing:.02em;pointer-events:none}
  @media(max-width:620px){#skyFoundationRelationships .sky-relationship-scope-filter{grid-column:1/-1}#skyFoundationRelationships .sky-relationship-scope-choices{justify-content:flex-start;overflow:visible}.sky-intrasky-generated::after{font-size:.45rem}}
`;document.head.appendChild(style)}
function merge(){queued=false;const list=document.getElementById('skyFoundationRelationshipList');if(!list||!window.RelphiHarmonicOrb)return;installStyles();annotateInterRows(list);list.querySelectorAll(':scope>.sky-intrasky-generated').forEach(row=>row.remove());const a=relationships(prepare('A'),'intra-a'),b=relationships(prepare('B'),'intra-b');a.forEach((relation,index)=>list.appendChild(makeRow(relation,index)));b.forEach((relation,index)=>list.appendChild(makeRow(relation,index)));sortRows(list);ensureScopeControl();applyScope(readScopes());scheduleColors();scheduleCount();attachObserver(list);document.documentElement.dataset.skyIntraskyRelationships=`A:${a.length};B:${b.length}`;window.dispatchEvent(new CustomEvent('relphi:sky-intrasky-relationships-ready',{detail:{A:a.length,B:b.length,total:a.length+b.length}}))}
function schedule(){if(queued)return;queued=true;requestAnimationFrame(()=>requestAnimationFrame(merge))}
function attachObserver(list){if(listObserver?.__list===list)return;listObserver?.disconnect();listObserver=new MutationObserver(records=>{let needsColor=false,needsCount=false;for(const record of records){if(record.type==='attributes'&&record.attributeName==='data-template-key')needsColor=true;if(record.type==='attributes'&&['class','hidden'].includes(record.attributeName))needsCount=true;if(record.type==='childList'){needsColor=true;needsCount=true}}if(needsColor)scheduleColors();if(needsCount)scheduleCount()});listObserver.__list=list;listObserver.observe(list,{subtree:true,childList:true,attributes:true,attributeFilter:['class','hidden','data-template-key']})}
function start(){installStyles();schedule();['relphi:sky-foundation-interactions-ready','relphi:sky-foundation-ready'].forEach(name=>window.addEventListener(name,schedule));['relphi:sky-placement-multiselect-changed','relphi:sky-house-multiselect-changed','relphi:sky-aspect-multiselect-changed','relphi:sky-zodiac-filter-changed','relphi:sky-harmonic-window-visibility-changed','relphi:relationship-display-changed'].forEach(name=>window.addEventListener(name,scheduleCount));window.addEventListener('storage',event=>{if(event.key===SCOPE_KEY)applyScope(readScopes())})}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start,{once:true}):start();
})();
