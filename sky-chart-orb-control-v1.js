// Harmonic phase-window control. The visible number is the master harmonic window, not a raw aspect orb.
(function(){
  'use strict';
  if(!/(^|\/)sky-chart\.html$/.test(location.pathname))return;
  if(window.__relphiSkyOrbControlV5)return;
  window.__relphiSkyOrbControlV5=true;
  window.__relphiSkyOrbControlV4=true;
  window.__relphiSkyOrbControlV3=true;
  window.__relphiSkyOrbControlV2=true;
  window.__relphiSkyOrbControlV1=true;
  let queued=false,wheelIndexes=null,wheelState=null;
  const model=()=>window.RelphiHarmonicOrb;
  function phaseFromRow(row){const explicit=Number(row.dataset.phaseError);if(Number.isFinite(explicit))return explicit;const orb=Number(row.dataset.sourceOrb??row.dataset.orb),harmonic=Number(row.dataset.harmonicOrder);return Number.isFinite(orb)&&Number.isFinite(harmonic)&&harmonic>0?Math.abs(orb*harmonic):NaN}
  function setSvgVisibility(node,visible){node.hidden=!visible;node.classList.toggle('sky-chart-orb-hidden',!visible);node.setAttribute('aria-hidden',visible?'false':'true');if(visible){node.style.removeProperty('display');node.style.removeProperty('pointer-events')}else{node.style.setProperty('display','none','important');node.style.setProperty('pointer-events','none','important')}}
  function reconcilePlacementIsolation(rows,visibleIndexes){if(wheelState?.kind!=='placement')return;const kept=new Set([`${wheelState.sky}:${wheelState.value}`]);rows.forEach(row=>{if(!visibleIndexes.has(String(row.dataset.relationIndex)))return;kept.add(`A:${row.dataset.leftPlacement}`);kept.add(`B:${row.dataset.rightPlacement}`)});document.querySelectorAll('#skyFoundationWheelMount [data-focus-piece="placement"],#skyFoundationWheelMount [data-focus-piece="leader"]').forEach(node=>node.classList.toggle('is-kept',kept.has(`${node.dataset.sky}:${node.dataset.placement}`)))}
  function apply(){queued=false;const input=document.querySelector('[data-filter="orb"]');if(!input)return;const raw=input.value.trim(),limit=Number(raw),max=model()?.maxWindow??12,valid=raw!==''&&Number.isFinite(limit)&&limit>=0&&limit<=max;input.setCustomValidity(valid?'':`Enter a harmonic phase window from 0 to ${max} degrees.`);if(!valid)return;const visibleIndexes=new Set(),rows=[...document.querySelectorAll('.sky-foundation-relationship-row[data-relation-index]')];rows.forEach(row=>{const phase=phaseFromRow(row),hiddenByOrb=Number.isFinite(phase)&&phase>limit,hiddenByWheel=wheelIndexes&&!wheelIndexes.has(String(row.dataset.relationIndex)),hiddenByOther=row.classList.contains('sky-chart-filter-hidden')||row.classList.contains('sky-chart-multiselect-hidden')||row.classList.contains('sky-chart-house-multiselect-hidden')||row.classList.contains('sky-chart-aspect-multiselect-hidden')||row.classList.contains('sky-foundation-single-sky-cross-hidden'),visible=!hiddenByOrb&&!hiddenByWheel&&!hiddenByOther;row.classList.toggle('sky-chart-orb-hidden',hiddenByOrb);row.hidden=!visible;row.setAttribute('aria-hidden',visible?'false':'true');if(visible)visibleIndexes.add(String(row.dataset.relationIndex))});document.querySelectorAll('[data-layer="aspects"] .sky-foundation-aspect').forEach(line=>{const index=String(line.dataset.relationIndex||'');setSvgVisibility(line,index!==''&&visibleIndexes.has(index))});reconcilePlacementIsolation(rows,visibleIndexes);const count=document.getElementById('skyFoundationRelationshipCount'),empty=document.getElementById('skyFoundationRelationshipEmpty');if(count)count.textContent=`${visibleIndexes.size}/${rows.length}`;if(empty)empty.hidden=visibleIndexes.size!==0;document.documentElement.dataset.skyHarmonicWindow=String(limit)}
  function schedule(){if(queued)return;queued=true;requestAnimationFrame(apply)}
  function announceLimit(input){const max=model()?.maxWindow??12,value=Number(input.value);if(!Number.isFinite(value)||value<0||value>max)return;window.dispatchEvent(new CustomEvent('relphi:sky-orb-limit-changed',{detail:{orb:value,harmonicWindow:value,mode:'harmonic-phase'}}))}
  function install(){const bar=document.querySelector('.sky-chart-filter-bar');if(!bar||bar.querySelector('[data-filter="orb"]'))return;const field=document.createElement('label');field.className='sky-orb-number-field';const caption=document.createElement('span');caption.textContent='Harmonic window';const input=document.createElement('input'),m=model();input.type='number';input.min='0';input.max=String(m?.maxWindow??12);input.step='0.1';input.inputMode='decimal';input.value=String(m?.defaultWindow??6);input.dataset.filter='orb';input.dataset.orbMode='harmonic-phase';input.setAttribute('aria-label',`Master harmonic phase window in degrees, maximum ${input.max}`);field.append(caption,input);bar.prepend(field);input.addEventListener('input',()=>{announceLimit(input);schedule()});input.addEventListener('change',()=>{const v=Number(input.value),max=Number(input.max);if(Number.isFinite(v))input.value=String(Math.max(0,Math.min(max,v)));announceLimit(input);schedule()});announceLimit(input);schedule()}
  function start(){install();window.addEventListener('relphi:sky-foundation-interactions-ready',()=>{install();schedule()});window.addEventListener('relphi:sky-foundation-filter-changed',event=>{wheelState=event.detail?.state||null;wheelIndexes=wheelState?.mode==='selected'?new Set((event.detail.relationshipIndexes||[]).map(String)):null;schedule()});['relphi:sky-placement-multiselect-changed','relphi:sky-house-multiselect-changed','relphi:sky-aspect-multiselect-changed','relphi:selected-relationship-rendered','relphi:sky-foundation-ready'].forEach(name=>window.addEventListener(name,schedule));document.getElementById('skyFoundationRelationships')?.addEventListener('change',schedule)}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start,{once:true}):start();
})();